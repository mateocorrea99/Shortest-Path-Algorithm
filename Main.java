package a5;

public class Main {


    public static void main(String[] args) {
        //You are encouraged (but not required) to include your testing code here.

        //Hint: Try to test basic operations (e.g., adding a few nodes and edges to graphs)
        //before you implement more complex methods

        /*Graph graph = new GraphImpl();
        graph.addNode("a"); graph.addNode("b"); graph.addNode("c"); graph.addNode("d");
        graph.addEdge("a", "b", 1); graph.addEdge("b", "c", 2);
        graph.addEdge("c", "d", 3); graph.addEdge("d", "a", 4);
        graph.addEdge("d", "c", 6); graph.addEdge("a", "d", 2);
        graph.addEdge("c", "b", 1); graph.addEdge("b", "d", 2);
        graph.addEdge("a", "c", 3);
        graph.graphVisualize();

        Map<String, Double> results = graph.dijkstra("a");*/

    }

}
